/*name:
 * lab section:
 * assignment:
 * last worked on:
 * due date:
 * file purpose: This is the header file for the numArray module.
 */

#ifndef NUMARRAY_H
#define NUMARRAY_H

/* This function fills an array (myNums) of size with integer numbers
   by user input.
   Precondition: array must have the size passed in
   Post condition: array is filled with user input.  */
void numFill(int *myNums, int size);

/* This function prints an array (myNums) of size to standard output
   Precondition: array must have the size passed in
   Post condition: array is printed.  */
void numPrint(int *myNums, int size);

#endif /* NUMARRAY_H */










